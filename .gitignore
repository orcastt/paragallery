.DS_Store
Thumbs.db
node_modules/

# manifest.json is generated by the GitHub Action into the Pages artifact.
# Keeping it out of git avoids a committed copy drifting out of sync with the
# deployed one. Run `node scripts/build-manifest.js` for local preview.
manifest.json
